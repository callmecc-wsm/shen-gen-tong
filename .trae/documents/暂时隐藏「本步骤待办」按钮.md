## PM 概述
- 目标：暂时隐藏步骤页右上角的「本步骤待办」入口按钮，避免用户误触；功能后续优化再恢复。
- 影响：仅隐藏入口，不改动待办侧拉窗逻辑与数据；页面其他功能（进度条、导航、Markdown内容等）不受影响。

## 技术方案
- 方法对比：
  - A（MVP/速度）：直接注释掉按钮的 JSX 代码块，最快、风险最低。
  - B（可扩展）：保留代码，改为受“feature flag”（如 `SHOW_TODO_ENTRY`）控制的条件渲染，便于灰度开启。
- 推荐：A。当前只是临时隐藏，注释即可；后续若需灰度再切换到 B。

## 修改文件
- `app/step/[id]/StepContent.tsx`
  - 位置：标题卡片内按钮元素，参考行号 85–93。

## 具体改动
- 将按钮 JSX 注释掉，并加入中文业务注释说明是临时隐藏：
  - 代码位置：`/Users/bytedance/Documents/shengentong - 4/app/step/[id]/StepContent.tsx:85-93`
  - 改动内容：用 `{/* ... */}` 包裹并标注「暂时隐藏：本步骤待办入口，待后续优化恢复」。
- 保留 `isTodoOpen` 状态与 `<TodoSidebar />` 组件，不做删除，以免后续恢复复杂。
- 视觉检查：按钮移除后标题行布局仍为左右结构；如需更紧凑，可将父容器 `gap-4` 调整为 `gap-3`（本次先不改，观察实际效果）。

## 验证步骤
- 启动本地开发服务并打开任一步骤页：按钮不显示；其他交互正常。
- 断开登录态与恢复登录态流程验证不受影响（该按钮不涉鉴权）。
- 代码回滚方便：取消注释即可恢复按钮。

## 交付
- 完成后提供一次性 `git` 提交命令（中文描述），便于 PM 执行。