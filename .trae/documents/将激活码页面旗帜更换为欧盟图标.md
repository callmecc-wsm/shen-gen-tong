## PM总结（做什么 & 为什么）
- 将“激活码”页面顶部的意大利国旗替换为欧盟旗帜，提升品牌一致性与专业感。
- 同步检查概览页是否也使用意大利旗，统一替换为欧盟旗帜。

## 技术方案（怎么做）
- 目标文件与位置：
  - 激活码页：`app/page.tsx:116` 当前为 emoji `"🇮🇹"`。
  - 概览页（若需统一）：`app/overview/OverviewClient.tsx:115` 也为 emoji。
- 资源与渲染：
  - 在 `public/flags/eu.svg` 添加欧盟旗 SVG（蓝底12星）。
  - 在上述页面引入 `next/image`，用 `<Image src="/flags/eu.svg" alt="欧盟旗帜" width={72} height={72} priority />` 替换原 emoji。
- 验证点：
  - 图标尺寸约等于原 `text-6xl` 视觉，居中对齐不变。
  - 无障碍 `alt` 文本为“欧盟旗帜”。
  - 移动端布局与交互无变化。
- 输出：完成后本地自测通过，并提供一次性提交命令：
  ```bash
  git add .
  git commit -m "feat: 将激活码与概览页旗帜统一为欧盟SVG图标"
  ```

请确认开始执行方案B的实现与替换（激活码页必做，概览页可一并统一）。