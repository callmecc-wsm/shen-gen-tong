# 本地部署说明

## ✅ 已完成配置

您的项目现在已经可以在本地运行了！

### 📁 已创建的文件

1. **`.env.local`** - 本地环境变量配置（包含您的 InstantDB 凭证）
2. **`.env.example`** - 环境变量模板（供以后参考）

### 📦 已安装的依赖

- `@instantdb/react` - InstantDB React 客户端
- `@instantdb/admin` - InstantDB Admin 工具（用于后端 API）
- `jsonwebtoken` - JWT 令牌生成和验证
- `@types/jsonwebtoken` - TypeScript 类型定义
- `tsx` - TypeScript 执行器（用于运行脚本）
- `dotenv` - 环境变量加载器（生成激活码脚本需要）

---

## 📥 快速部署步骤（新电脑必看）

### 第一步：安装依赖

```bash
npm install
npm install dotenv  # 生成激活码功能必需
```

💡 **为什么需要 dotenv？**
想象您有一本私密笔记本（.env.local 文件），里面记录了重要的账号密码。
dotenv 就像一个"管家"，负责读取这本笔记本的内容，让程序能使用里面的信息。
没有这个"管家"，程序就读不到账号密码，功能就无法运行。

### 第二步：创建环境变量文件

创建 `.env.local` 文件，填入以下内容：

```env
NEXT_PUBLIC_INSTANTDB_APP_ID=a95b5253-ff7c-43c8-a67d-edf95aa0d217
INSTANTDB_ADMIN_TOKEN=b9050311-bd71-4977-8075-7c1e26660826
JWT_SECRET=schengen_visa_helper_secret_key_2025_change_in_production
```

### 第三步：启动项目

```bash
npm run dev
```

浏览器访问：http://localhost:3000

---

## 🚀 如何启动项目

### 方法 1：开发模式（推荐）

```bash
npm run dev
```

然后在浏览器中访问：http://localhost:3000

### 方法 2：生产构建

```bash
# 构建项目
npm run build

# 启动生产服务器
npm start
```

---

## 🔧 环境变量说明

您的 `.env.local` 文件包含以下配置：

```env
NEXT_PUBLIC_INSTANTDB_APP_ID=a95b5253-ff7c-43c8-a67d-edf95aa0d217
INSTANTDB_ADMIN_TOKEN=b9050311-bd71-4977-8075-7c1e26660826
JWT_SECRET=schengen_visa_helper_secret_key_2025_change_in_production
```

### 变量说明

| 变量名 | 说明 | 安全级别 |
|--------|------|---------|
| `NEXT_PUBLIC_INSTANTDB_APP_ID` | InstantDB 应用 ID（前端可见） | 公开 |
| `INSTANTDB_ADMIN_TOKEN` | 管理员令牌（仅后端使用） | ⚠️ 私密 |
| `JWT_SECRET` | JWT 签名密钥（生成激活码令牌） | ⚠️ 私密 |

⚠️ **安全提示**：`.env.local` 文件已被 `.gitignore` 忽略，不会提交到 Git 仓库。

---

## 📋 生成激活码（可选）

如果您需要生成新的激活码：

```bash
# 生成 10 个默认激活码
npx tsx scripts/generate-codes.ts

# 生成 100 个激活码，有效期 365 天
npx tsx scripts/generate-codes.ts 100 schengen 365
```

---

## 🧪 测试功能

启动项目后，您可以：

1. ✅ 访问首页（http://localhost:3000）
2. ✅ 输入激活码测试
3. ✅ 选择国家（意大利）
4. ✅ 查看步骤总览
5. ✅ 测试进度保存功能

---

## 🆚 本地 vs Vercel 的区别

| 特性 | 本地开发 | Vercel 部署 |
|------|---------|------------|
| **访问方式** | http://localhost:3000 | your-app.vercel.app |
| **环境变量** | `.env.local` 文件 | Vercel Dashboard 配置 |
| **热更新** | ✅ 自动刷新 | ❌ 需要重新部署 |
| **适用场景** | 开发、测试、调试 | 生产、分享给用户 |

---

## ❓ 常见问题

### Q1: 端口 3000 已被占用怎么办？

```bash
# 使用其他端口启动
npm run dev -- -p 3001
```

### Q2: 修改 .env.local 后不生效？

重启开发服务器：

```bash
# 按 Ctrl+C 停止，然后重新启动
npm run dev
```

### Q3: 如何查看控制台日志？

开发服务器的日志会在终端中显示，前端的日志在浏览器开发者工具的 Console 中查看。

---

## 📚 下一步

现在您已经可以在本地开发和测试了！如果需要：

1. **修改代码** → 保存后自动刷新
2. **生成激活码** → 使用上面的命令
3. **部署到 Vercel** → 参考 `DEPLOYMENT_GUIDE.md`

祝开发愉快！🎉

